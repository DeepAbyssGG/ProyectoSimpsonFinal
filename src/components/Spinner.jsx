import "./spinner.css";

const Spinner = () => {
  return (
    <section classNameName="d-flex justify-content-center">
      <div className="spinner"></div>
    </section>
  );
};

export default Spinner;
